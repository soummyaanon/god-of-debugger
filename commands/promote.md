---
description: Promote surviving falsification experiments into permanent regression tests after a fix lands.
---

Invoke the `promote` skill from the god-of-debugger plugin. Re-run surviving experiments against the fixed tree, keep those that failed without the fix and pass with it, and promote them into the repo's test suite. Follow the skill's instructions exactly.
